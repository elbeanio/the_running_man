{
    "$schema": "https://opencode.ai/config.json"
    // Running Man no longer ships an MCP server. Agents use its REST API on
    // http://localhost:9000 (self-describing: GET / lists endpoints, /docs serves
    // OpenAPI), guided by the skill in .opencode/skills/.
    // See PROJECT.md for why MCP was removed.
}
